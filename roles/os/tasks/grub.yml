---
- name: Lower GRUB_TIMEOUT value
  ansible.builtin.lineinfile:
    path: /etc/default/grub
    regexp: "^GRUB_TIMEOUT="
    line: "GRUB_TIMEOUT={{ os_grub_timeout }}"
    state: present
    backup: true
  notify: Update grub
